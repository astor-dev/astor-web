---
// src/pages/new-project.astro
import Layout from "~layouts/BaseLayout.astro";
import PostForm from "~components/Form/PostForm";
import AdminOnly from "~components/Wrapper/AdminOnly";
---

<Layout title="새 포스트">
  <AdminOnly client:load>
    <div class="mx-auto w-full max-w-5xl px-4 py-8 sm:px-6 lg:px-8">
      <!-- 헤더 -->
      <div class="mb-8">
        <h1 class="text-3xl font-bold text-black-accent">새 포스트</h1>
        <p class="mt-2 text-black-muted">새로운 포스트를 등록합니다</p>
      </div>
      <PostForm client:load />
    </div>
  </AdminOnly>
</Layout>
